%%%% LLARS Demo Paper - IJCAI-ECAI 2026

\typeout{LLARS Demo Paper for IJCAI-ECAI 2026}

\documentclass{article}
\pdfpagewidth=8.5in
\pdfpageheight=11in

\usepackage{ijcai26}
\usepackage{times}
\usepackage{soul}
\usepackage{url}
\usepackage[hidelinks]{hyperref}
\usepackage[utf8]{inputenc}
\usepackage[small]{caption}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, fit, backgrounds, calc}

\urlstyle{same}

\pdfinfo{
/TemplateVersion (IJCAI.2026.0)
}

\title{LLARS: An End-to-End Pipeline for Collaborative Prompting, Batch Generation, and Hybrid Evaluation}

\author{
Philipp Steigerwald$^1$\and
Mara Stieler$2$\and
Jennifer Burghardt$2$\and
Eric Rudolph$^1$\and
Jens Albrecht$^1$\\
\affiliations
Technische Hochschule Nürnberg Georg Simon Ohm, Nürnberg, Germany\\
$^1$Faculty of Computer Science, Center for Artificial Intelligence (KIZ)\\
$^2$Faculty of Social Sciences, Institute for E-Counselling
\emails
\{philipp.steigerwald, mara.stieler, jennifer.burghardt, eric.rudolph, jens.albrecht\}@th-nuernberg.de
}

\begin{document}

\maketitle

\begin{abstract}
We demonstrate LLARS, an open-source platform that bridges the gap between domain experts and developers for systematic evaluation of LLM outputs.
The platform provides three integrated modules that can be used together or independently:
\textbf{Collaborative Prompt Engineering} lets domain experts and developers co-author prompts in a shared real-time workspace with version control and instant LLM testing.
\textbf{Batch Generation} systematically produces outputs for all combinations of prompts, LLMs, and data items with built-in cost tracking.
\textbf{Large-Scale Evaluation} supports human and LLM evaluators with live agreement metrics---applicable to outputs from any source.
When combined, these modules form a complete pipeline from prompt development through rigorous assessment.

\vspace{0.3em}
\noindent\textbf{Demo:} \url{https://llars-project.github.io/demo} % TODO: Set up GitHub Pages demo site before submission
\end{abstract}

\section{Introduction}

\definecolor{clrPrompt}{HTML}{B0CA97}
\definecolor{clrBatch}{HTML}{88C4C8}
\definecolor{clrEval}{HTML}{D1BC8A}
\definecolor{clrOutcome}{HTML}{E8A087}

\begin{figure}[t]
\centering
\begin{tikzpicture}[
    module/.style={
        rectangle, minimum width=3.2cm, minimum height=1.0cm,
        align=center, font=\footnotesize\bfseries,
        rounded corners=3pt, line width=0.9pt
    },
    actor/.style={
        rectangle, draw=black!50, fill=black!6,
        minimum width=2.2cm, minimum height=0.7cm,
        align=center, font=\fontsize{6.5}{8}\selectfont,
        line width=0.5pt
    },
    export/.style={
        rectangle, draw=black!25, fill=black!4,
        minimum width=1.5cm, minimum height=0.45cm,
        align=center, font=\fontsize{6}{7.5}\selectfont,
        rounded corners=1pt
    },
    outcome/.style={
        rectangle, draw=clrOutcome!70!black, fill=clrOutcome!20,
        minimum width=3.2cm, minimum height=1.0cm,
        align=center, font=\footnotesize\bfseries,
        rounded corners=3pt, line width=0.9pt
    },
    arrow/.style={-{Stealth[length=4.5pt]}, line width=0.7pt, black!55},
    pipearrow/.style={-{Stealth[length=5pt, width=4pt]}, line width=1.0pt, black!65},
    dasharrow/.style={-{Stealth[length=3.5pt]}, line width=0.5pt, black!30, dashed},
    lbl/.style={font=\fontsize{6}{7.5}\selectfont, text=black!55},
]

% === Module 1: Prompt Engineering ===
\node[module, draw=clrPrompt!70!black, fill=clrPrompt!25] (pe) at (0, 0)
    {Collaborative\\[-1pt]Prompt Engineering};

% === Module 2: Batch Generation ===
\node[module, draw=clrBatch!70!black, fill=clrBatch!25] (bg) at (0, -1.65)
    {Batch\\[-1pt]Generation};

% === Module 3: Evaluation ===
\node[module, draw=clrEval!70!black, fill=clrEval!25] (ev) at (0, -3.3)
    {Evaluation};

% === Actors (left side, more spacing from LLARS box) ===
\node[actor] (dev) at (-3.7, 0) {Domain Experts\\[-1pt]\& Developers};
\node[actor] (data) at (-3.7, -1.65) {Domain\\[-1pt]Data};
\node[actor] (llmeval) at (-3.7, -3.3) {Human \&\\[-1pt]LLM Evaluators};

% === Actor arrows ===
\draw[arrow] (dev.east) -- (pe.west);
\draw[arrow] (data.east) -- (bg.west);
\draw[arrow] (llmeval.east) -- (ev.west);

% === Pipeline arrows ===
\draw[pipearrow] (pe.south) -- node[lbl, right, xshift=1pt] {Prompts} (bg.north);
\draw[pipearrow] (bg.south) -- node[lbl, right, xshift=1pt] {Outputs} (ev.north);

% === Export arrows (to the right, with more spacing) ===
\node[export] (exp1) at (3.4, 0) {Prompt};
\node[export] (exp2) at (3.4, -1.65) {LLM Outputs};
\node[export] (exp3) at (3.4, -3.3) {Evaluation\\[-1pt]Results};
\draw[dasharrow] (pe.east) -- (exp1);
\draw[dasharrow] (bg.east) -- (exp2);
\draw[dasharrow] (ev.east) -- (exp3);

% === Outcome (bottom, more spacing from LLARS box) ===
\node[outcome] (outcome) at (0, -5.05) {Best LLM \\[-1pt]\& Prompt Pair};
\draw[pipearrow] (ev.south) -- (outcome.north);

% === LLARS bounding box (light green tint) ===
\begin{scope}[on background layer]
\node[draw=clrPrompt!60!black!40, dashed, rounded corners=6pt, line width=0.9pt,
      fill=clrPrompt!5,
      fit=(pe)(bg)(ev),
      inner xsep=10pt, inner ysep=10pt,
     ] (llarsbox) {};
\node[font=\fontsize{7}{9}\selectfont\bfseries, text=clrPrompt!60!black!50,
      anchor=south east] at ([xshift=-5pt, yshift=-1pt]llarsbox.south east) {LLARS};
\end{scope}

\end{tikzpicture}
\caption{LLARS pipeline: domain experts and developers collaboratively develop prompts, generate outputs across LLMs, and evaluate results with human and LLM evaluators. Each stage supports export; the pipeline yields a validated model--prompt combination.}
\label{fig:pipeline}
\end{figure}

Organizations increasingly build LLM-based systems for sensitive domains such as legal analysis, medical documentation, and psychosocial counseling.
Developing these systems requires writing effective prompts, selecting suitable models, and rigorously evaluating outputs---tasks that demand both domain expertise and technical skill.
Domain experts understand \emph{what} constitutes quality in their field, while developers know \emph{how} to structure prompts and configure LLM pipelines.
Yet in practice, prompts are revised through multiple rounds of feedback in shared documents without version control or a common testing environment, leaving teams unsure which version was tested against which model.
At the same time, organizations need to determine the right model for their use case---comparing open-source alternatives for privacy-preserving on-premise deployment or finding the most cost-effective proprietary option---but lack tools for systematic, collaborative comparison.
Existing platforms address only fragments of this workflow:
annotation tools like Label Studio~\cite{labelstudio2024} and Argilla~\cite{argilla2024} provide evaluation interfaces but no collaborative prompt development;
LLM-as-Judge frameworks like MT-Bench~\cite{zheng2024judging} automate assessment but exclude human evaluators.
No existing tool offers a unified pipeline from collaborative prompt engineering through systematic output generation to hybrid human--LLM evaluation.

We present LLARS (LLM Assisted Rating System), an open-source platform that closes this gap with an integrated three-stage pipeline (Figure~\ref{fig:pipeline}).
In the first stage, domain experts and developers \emph{collaboratively engineer prompts} in a shared real-time editor with built-in version control, testing candidate prompts against live LLMs without leaving the workspace.
Prompts can be exported for external use or passed directly into the second stage, where \emph{batch generation} produces outputs for all combinations of data items, prompts, and models with built-in cost estimation.
In the third stage, both human and LLM evaluators \emph{assess the outputs} within the same framework---rating, ranking, comparing, or labeling---while real-time agreement metrics reveal where human and machine judgment converge or diverge.

For example, a legal team building an information extraction system can use the collaborative editor to refine prompts with legal experts, then run batch generation across candidate models---from large proprietary to small open-source alternatives---over hundreds of documents, and finally have legal experts and LLM evaluators jointly assess whether each model reliably identifies relevant clauses rather than hallucinating when information is absent.
This end-to-end workflow directly informs which model--prompt combination to deploy.

Each stage also operates independently, and results are exportable as JSON or CSV at every point.
The platform connects to any OpenAI-compatible LLM provider---including locally hosted models via Ollama or vLLM---and is fully open-source.


\section{Platform Overview}

LLARS is a containerized web application built with Vue.js (frontend), Flask (backend), and MariaDB (storage).
Real-time collaboration is provided by a dedicated Yjs~\cite{jahns2023yjs} server, and LLM access follows the OpenAI-compatible API structure, allowing teams to connect commercial providers (OpenAI, Anthropic, Google) as well as locally hosted models via Ollama.
All interactions---prompt editing, generation progress, and evaluation updates---are synchronized via WebSocket.

The platform provides three modules that can be used independently or combined into a pipeline.
Prompts created in the collaborative editor feed into batch generation, which produces outputs that are then assessed in the evaluation module.
At each stage, results can be exported as JSON or CSV for external use.

\subsection{Collaborative Prompt Engineering}

Developing effective prompts for domain-specific LLM systems requires iterative refinement by those who understand the domain and those who understand prompt design.
The prompt engineering module (Figure~\ref{fig:prompt-editor}) addresses this through a real-time collaborative editor built on Yjs~\cite{jahns2023yjs}, a CRDT framework that synchronizes edits at character granularity, merging concurrent changes without conflicts.

Users compose prompts as reorderable message blocks (e.g., system instructions, user templates) and define template variables using \texttt{\{\{variable\}\}} syntax.
Variables are automatically extracted and displayed in a shared palette; during testing and batch generation, they are substituted with domain-specific data.
Any collaborator can test the current prompt independently: clicking \emph{Test} captures an atomic snapshot, fills variables with sample data, sends the prompt to a user-selected LLM, and streams the response---without interrupting concurrent editing.
Multiple users can test against different models or data in parallel, enabling rapid exploration of prompt behavior across configurations.

A git-inspired version control system records each commit with multi-author attribution, tracking per-user contributions at character level.
Teams can compare versions, inspect individual contributions, and roll back to previous states.
Finalized prompts are exported as JSON for integration into production systems or passed directly to the batch generation module.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/prompt-editor.png}
\caption{Collaborative prompt editor showing a news summarization prompt with two message blocks, template variables in the shared palette (left), and online presence. The sidebar provides testing, variable management, and JSON import/export.}
\label{fig:prompt-editor}
\end{figure}

\subsection{Batch Generation}

With prompts refined, the batch generation module scales output creation across combinations of data, prompts, and models (Figure~\ref{fig:batch-generation}).
A guided wizard walks users through source selection, prompt and model configuration, and parameter tuning including budget limits.
Sources can be existing evaluation scenarios, uploaded CSV or JSON files, or---for self-contained prompts that require no input data, such as creative text generation or instruction-following benchmarks---no source at all.
The system generates the full Cartesian product: $N$~data items $\times$ $M$~prompts $\times$ $K$~models.
Template variables in prompts are automatically matched to data columns by name---a CSV column \texttt{content} fills every \texttt{\{\{content\}\}} placeholder across all selected prompts.

Users can register custom LLM providers with their own API keys; these models then appear throughout the platform---in prompt testing, batch generation, and as LLM evaluators.
Before execution, LLARS estimates costs based on model pricing and token projections; optional budget caps automatically pause jobs that exceed thresholds.
All generation progress---including token-level output streaming---is broadcast via WebSocket, allowing team members to join and monitor running jobs in real time.
Each completed output retains full provenance---source item, prompt template, model, token count, and cost---exportable as CSV or JSON.
Completed batches can be transferred directly into the evaluation module through the integrated Scenario Wizard, preserving model attribution for systematic cross-model comparison.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/batch-generation.png}
\caption{Batch generation results for a news summarization job: two prompts tested across two LLMs on ten articles, producing 40~outputs with full provenance. The \emph{Scenario Wizard} button transfers outputs directly to evaluation.}
\label{fig:batch-generation}
\end{figure}

\subsection{Evaluation}

The evaluation module enables human evaluators and LLM evaluators to assess outputs within a unified framework, supporting four general evaluation types (Figure~\ref{fig:scenario-wizard}): \emph{rating} with configurable multi-dimensional Likert scales and weighted dimensions (e.g., coherence, fluency, relevance), \emph{ranking} where evaluators sort items into ordered buckets, \emph{labeling} for categorical classification (binary or multi-class), and pairwise \emph{comparison}.
The framework is extensible with domain-specific types; LLARS includes two for psychosocial counseling research (Section~\ref{sec:counseling}).

An AI-assisted Scenario Wizard guides scenario creation through five steps---data upload, task selection, configuration, team assembly, and review.
The wizard analyzes uploaded data structures using deterministic pattern matching with an LLM fallback for ambiguous cases, and recommends the most suitable evaluation type along with matching presets.
Scenarios can also be created directly from batch generation outputs, preserving model attribution so that evaluators compare outputs across LLMs for the same input.

Scenario owners control how items are distributed: all items to every evaluator for full inter-rater analysis, or configurable subsets for faster throughput.
Both human evaluators and LLM evaluators participate in the same scenarios; LLM evaluators use type-specific prompts that administrators can customize per scenario type.
As evaluations are submitted, LLARS computes agreement metrics in real time---Krippendorff's~$\alpha$, Cohen's~$\kappa$, Fleiss'~$\kappa$, Kendall's~$\tau$ and~$W$, Spearman's~$\rho$, and intraclass correlation~(ICC), among others---and scenario owners can filter by evaluator type (human--human, LLM--LLM, or human--LLM) to analyze where machine and human judgment diverge.
All evaluation data, metrics, and configurations can be exported as JSON or CSV for further analysis.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/szenario-wizard.png}
\caption{AI-assisted Scenario Wizard showing the task selection step with four general evaluation types and two domain-specific types for psychosocial counseling. The five-step wizard analyzes uploaded data and recommends the most suitable evaluation type.}
\label{fig:scenario-wizard}
\end{figure}

\section{Application: Online Counselling}
\label{sec:counseling}

LLARS is deployed in ongoing research on CAIA~\cite{steigerwald2025caia}, an AI-assisted support system for text-based psychosocial counselling with seven LLM-driven functionalities---from case summarisation to hypothesis generation.
Counselling researchers and developers used the collaborative prompt editor to jointly develop the prompts underlying these functionalities, testing iteratively against counselling data until outputs met professional standards.
For one of these functionalities---automated subject line generation~\cite{steigerwald2025subjectline}---the complete pipeline was exercised: batch generation produced 253~subject lines across 11~LLMs at different quantisation levels, and five counselling professionals together with an AI evaluator rated the outputs, yielding 1,518~assessments.
LLARS's cross-model comparison directly informed the selection of a privacy-preserving open-source model for local deployment.

These projects also motivated two domain-specific evaluation types (Figure~\ref{fig:scenario-wizard}):
\emph{Mail Rating} for assessing AI-generated counsellor responses on dimensions such as coherence and quality, and \emph{Authenticity} (Figure~\ref{fig:fake-real}) for classifying counselling threads as real or AI-generated, with ground truth labels enabling classification accuracy alongside inter-rater agreement.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/fake-real-interface.png}
\caption{Authenticity evaluation: evaluators read a counselling conversation and classify it as written by humans or containing AI-generated parts, with optional confidence and reasoning.}
\label{fig:fake-real}
\end{figure}

\section{Demonstration and Conclusion}

Our live demonstration walks attendees through the complete pipeline: collaboratively editing a prompt, generating outputs across multiple LLMs with real-time cost tracking, and evaluating results with both human and LLM evaluators while observing agreement metrics update live.
Attendees can bring their own data or use provided examples.
LLARS is open-source and runs via Docker (16\,GB RAM, 4+ CPU cores recommended).

% TODO: Uncomment and update URLs before submission
%\vspace{0.3em}
%\noindent\textbf{Repository:} \url{https://github.com/llars-project/llars}\\
%\textbf{Documentation:} \url{https://llars-project.github.io}

By integrating collaborative prompt engineering, systematic batch generation, and hybrid human--LLM evaluation into a single GUI-driven platform, LLARS addresses a gap in existing tooling: no prior system offers domain experts and developers a shared environment spanning the full cycle from prompt development to rigorous assessment.
Ongoing deployments in psychosocial online counselling demonstrate that this integrated pipeline supports real-world research workflows.
Future work includes a formal user study and extending the platform with fine-tuning integration and automated prompt optimization.

\bibliographystyle{named}
\bibliography{llars}

\end{document}