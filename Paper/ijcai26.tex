%%%% LLARS Demo Paper - IJCAI-ECAI 2026

\typeout{LLARS Demo Paper for IJCAI-ECAI 2026}

\documentclass{article}
\pdfpagewidth=8.5in
\pdfpageheight=11in

\usepackage{ijcai26}
\usepackage{times}
\usepackage{soul}
\usepackage{url}
\usepackage[hidelinks]{hyperref}
\usepackage[utf8]{inputenc}
\usepackage[small]{caption}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, fit, backgrounds, calc}

\urlstyle{same}

\pdfinfo{
/TemplateVersion (IJCAI.2026.0)
}

\title{LLARS: A Platform for Evaluating LLM Outputs\\with Human and Machine Evaluators}

\author{
Philipp Steigerwald$^1$\and
Jens Albrecht$^1$\\
\affiliations
$^1$Technische Hochschule N\"urnberg Georg Simon Ohm, Germany\\
\emails
\{philipp.steigerwald, jens.albrecht\}@th-nuernberg.de
}

\begin{document}

\maketitle

\begin{abstract}
We demonstrate LLARS, an open-source platform that bridges the gap between domain experts and AI developers for systematic evaluation of LLM outputs.
The platform provides three integrated modules that can be used together or independently:
\textbf{Collaborative Prompt Engineering} enables real-time co-editing with character-level tracking---useful for prompt optimization or compliance training under regulations like the EU AI Act~\cite{euaiact2024}.
\textbf{Batch Generation} scales output creation across multiple LLMs with cost tracking---enabling synthetic data generation or systematic model comparison.
\textbf{Large-Scale Evaluation} supports human and LLM evaluators with live agreement metrics---applicable to outputs from any source.
When combined, these modules form a complete pipeline from prompt development through rigorous assessment.

\vspace{0.3em}
\noindent\textbf{Demo:} \url{https://llars-project.github.io/demo} % TODO: Set up GitHub Pages demo site before submission
\end{abstract}

\section{Introduction}

Organizations increasingly build LLM-based systems for sensitive domains such as medical documentation, legal analysis, educational tutoring, and psychosocial counseling.
Developing these systems requires writing effective prompts, selecting suitable models, and rigorously evaluating outputs---tasks that demand both domain expertise and technical skill.
Domain experts understand \emph{what} constitutes quality in their field, while AI developers know \emph{how} to structure prompts and integrate LLM evaluators.
Yet existing tools force these groups into separate workflows:
annotation platforms like Label Studio~\cite{labelstudio2024} and Argilla~\cite{argilla2024} provide human evaluation interfaces but lack collaborative prompt development;
LLM-as-Judge frameworks like MT-Bench~\cite{zheng2024judging} automate assessment but require technical expertise and exclude human evaluators from the same workflow.
No existing tool offers a unified, GUI-driven pipeline from collaborative prompt development through systematic output generation to hybrid human-LLM evaluation.

We present LLARS (LLM Assisted Rating System), an open-source platform that integrates three modules---usable together as a pipeline or independently.

\textbf{Collaborative Prompt Engineering.}
Domain experts and AI developers co-edit prompts in a real-time collaborative workspace with CRDT-based synchronization and full version history.
Users define template variables (e.g., \texttt{\{\{content\}\}}, \texttt{\{\{title\}\}}) that serve as placeholders for evaluation data.
Each collaborator can independently test the current prompt at any time---variables are filled with uploaded data, the prompt is sent to the selected LLM, and the response is streamed back---without interrupting others.
Finalized prompts can be exported as JSON for direct integration into production systems.

\textbf{Batch Generation.}
Teams systematically generate outputs across all combinations of prompts, LLMs, and datasets.
A guided wizard lets users configure the generation matrix, estimate costs before execution, and enforce budget limits.
This enables rapid model screening---testing whether smaller, cheaper models produce acceptable outputs---as well as synthetic data generation for fine-tuning or quality assurance.
Generated outputs maintain full provenance and can be exported or passed directly into evaluation.

\textbf{Evaluation.}
Human evaluators and LLM evaluators assess outputs within the same framework, supporting rating, ranking, pairwise comparison, and labeling.
Scenario owners configure how items are distributed across evaluators---all items to everyone for full inter-rater analysis, or subsets for faster throughput.
LLARS computes agreement metrics (Krippendorff's $\alpha$, Cohen's $\kappa$, Fleiss' $\kappa$, Kendall's $\tau$, among others) in real-time across all evaluator combinations: human--human, LLM--LLM, and human--LLM.
An AI-assisted Scenario Wizard analyzes uploaded data and recommends the appropriate evaluation type based on data structure.

Each module can operate standalone: the prompt editor doubles as a training and compliance documentation environment, batch generation serves independent synthetic data creation, and the evaluation module accepts externally produced outputs.
Together, the modules form a complete pipeline where domain experts and AI developers iterate from prompt development through generation to rigorous assessment.
The platform supports over 100 LLM providers---including local deployments via Ollama---and is fully open-source.


%%% ===================================================================
%%% SECTION OUTLINE for 3 pages content + 2 pages references
%%% ===================================================================
%%%
%%% Page 1:
%%%   Section 1: Introduction                     (~1 column = text above)
%%%   Section 2: System Architecture              (~0.5 column)
%%%              - Architecture diagram (Figure 1)
%%%              - Tech stack: Vue.js, Flask, Yjs, LiteLLM, MariaDB
%%%
%%% Page 2:
%%%   Section 3: Platform Modules                 (~1.5 columns)
%%%     3.1 Collaborative Prompt Engineering
%%%              - Screenshot (Figure 2): prompt editor with variables
%%%              - Real-time CRDT sync, independent testing, version control
%%%     3.2 Batch Generation
%%%              - Matrix generation, cost control, provenance
%%%     3.3 Evaluation
%%%              - Screenshot (Figure 3): Scenario Wizard OR metrics dashboard
%%%              - Evaluation types, distribution settings, agreement metrics
%%%
%%% Page 3:
%%%   Section 4: Application: Counseling Evaluation (~0.5 column)
%%%              - Domain-specific types: Mail Rating, Authenticity
%%%              - Screenshot (Figure 4): Fake/Real labeling interface
%%%   Section 5: Demonstration                    (~0.3 column)
%%%              - What attendees will see, system requirements
%%%              - Repository + documentation links
%%%
%%% Pages 4--5:
%%%   References
%%%
%%% ===================================================================
%%% Total Figures: 3--4 (architecture, prompt editor, wizard/metrics,
%%%                       optional: labeling interface)
%%% ===================================================================


%% --- Section stubs below for reference ---

\section{System Architecture}
% Figure 1: Architecture diagram (existing TikZ figure)
% Brief description of tech stack

\section{Platform Modules}

LLARS provides three modules that can be used independently or combined into a pipeline from prompt development through output generation to evaluation.
Prompts created in the collaborative editor feed into batch generation, which produces outputs that are than assessed in the evaluation module.
At each stage, results can be exported as JSON or CSV for external use.
The following subsections detail each module.

\subsection{Collaborative Prompt Engineering}

Developing effective prompts for domain-specific LLM systems requires iterative refinement by those who understand the domain and those who understand prompt design.
The prompt engineering module (Figure~\ref{fig:prompt-editor}) addresses this through a real-time collaborative editor built on Yjs~\cite{jahns2023yjs}, a CRDT framework that synchronizes edits at character granularity, merging concurrent changes without conflicts.

Users compose prompts as reorderable message blocks (e.g., system instructions, user templates) and define template variables using \texttt{\{\{variable\}\}} syntax.
Variables are automatically extracted and displayed in a shared palette; during testing and batch generation, they are substituted with domain-specific data.
Any collaborator can test the current prompt independently: clicking \emph{Test} captures an atomic snapshot, fills variables with sample data, sends the prompt to a user-selected LLM, and streams the response---without interrupting concurrent editing.
Multiple users can test against different models or data in parallel, enabling rapid exploration of prompt behavior across configurations.

A git-inspired version control system records each commit with multi-author attribution, tracking per-user contributions at character level.
Teams can compare versions, inspect individual contributions, and roll back to previous states.
Finalized prompts are exported as JSON for integration into production systems or passed directly to the batch generation module.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/prompt-editor.png}
\caption{Collaborative prompt editor showing a news summarization prompt with two message blocks, template variables in the shared palette (left), and online presence. The sidebar provides testing, variable management, and JSON import/export.}
\label{fig:prompt-editor}
\end{figure}

\subsection{Batch Generation}
% Matrix: N items × M prompts × K models
% Wizard configuration, cost estimation, budget limits
% Provenance tracking, export (CSV/JSON), direct handoff to evaluation

\subsection{Evaluation}
% Figure 3: Scenario Wizard (evaluation type selection) OR agreement metrics dashboard
% Four types: rating, ranking, labeling, comparison
% Distribution settings: all-to-everyone, random, sequential
% Human + LLM evaluators, live agreement metrics
% Configurable rating dimensions with custom Likert scales

\section{Application: Counseling Evaluation}
% Figure 4: Fake/Real labeling interface (Authenticity scenario)
% Domain-specific types: Mail Rating, Authenticity
% Ongoing deployment in psychosocial online counseling project

\section{Demonstration}
% Workflow walkthrough
% System requirements: Docker, 16GB RAM, 4+ cores
% Links: repo, documentation, demo

\bibliographystyle{named}
\bibliography{llars}

\end{document}