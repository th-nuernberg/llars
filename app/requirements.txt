Flask==3.0.3
Flask-Cors==4.0.2
Flask-Login==0.6.3
Flask-SQLAlchemy==3.1.1
Flask-Limiter==3.5.0
pymysql==1.1.1
Werkzeug==3.0.6  # Pin: 3.1+ makes session read-only property, breaks Flask-SocketIO
Flask-JWT-Extended==4.6.0  # Legacy auth routes - TODO: migrate fully to Authentik
Flask-Migrate
flask-socketio==5.4.1
python-engineio==4.13.0
eventlet==0.33.3
gevent==24.2.1  # Production async worker (alternative to eventlet)
gevent-websocket==0.10.1  # WebSocket support for gevent
gunicorn==22.0.0  # Production WSGI server
openai==1.75.0
requests==2.33.0

# OIDC Integration (legacy Keycloak compatibility)
python-keycloak==5.0.0
PyJWT==2.12.0
cryptography==46.0.6

# Authentik Integration
psycopg2-binary==2.9.9

langchain==0.3.27
langchain-community==0.3.27
langchain-core==0.3.81
langchain-openai==0.2.9
chromadb==0.5.20
# Pin posthog to avoid breaking API changes (chromadb telemetry expects the legacy capture signature)
posthog==5.4.0
python-dotenv==1.0.1
sentence-transformers==3.3.1
transformers==4.46.3
torch==2.5.1
PyPDF2==3.0.1
pypdf==6.9.2
langchain-huggingface==0.1.2
langchain-chroma==0.1.4

y-py==0.6.2
python-socketio==5.14.0
python-dateutil==2.9.0

# Offline Anonymize Tool (NER + Recommender)
flair==0.15.1
scikit-learn==1.5.0
numpy>=1.26.0  # Explicit: used for vectorized agreement metrics (Krippendorff, etc.)

# System monitoring for OnCoCo
psutil==6.1.0
docker==7.1.0

# Web Crawler
beautifulsoup4==4.12.3
lxml==5.3.0
playwright==1.49.1
aiohttp==3.13.3  # Async HTTP for Playwright image downloads

# Document Processing (PDF, Word, Excel, PowerPoint)
PyMuPDF==1.24.10
python-docx==1.1.2
openpyxl==3.1.5
python-pptx==1.0.2
Pillow==12.1.1

# Conference Wizard (DuckDuckGo search)
ddgs>=7.0.0

# API key hashing (PHC winner, timing-safe verification)
argon2-cffi==23.1.0

# Redis for server-authoritative sessions (Wizard Sessions, etc.)
redis==5.0.1

# LiveKit (Voice/Video calls) — requires Python 3.11+, disabled until base image updated
# livekit-server-sdk>=0.7.0
# PyNaCl>=1.5.0
