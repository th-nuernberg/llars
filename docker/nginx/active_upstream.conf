# Blue-Green active upstream (managed by deploy_bluegreen.sh)
# DO NOT EDIT MANUALLY - updated by deploy_bluegreen.sh
#
# Initial config points to regular service names for backward compatibility.
# After first blue-green deploy, this will point to colored containers
# (e.g., llars_flask_blue, llars_frontend_blue, llars_yjs_blue).

upstream frontend {
    server frontend-vue-service:5173;
}

upstream backend {
    server backend-flask-service:8081;
}

upstream yjs {
    server yjs-service:8082;
}
